{% extends 'templates/_document.twig' %}

{% block Content %}
  {% for component in post.meta('pageSecondaryNavigationComponents') %}
    {{ renderComponent(component) }}
  {% endfor %}
  {{ renderComponent('SecondaryNavigation', { menu_id: post.menu_id, nav_enabled: post.nav_enabled }) }}
{% endblock %}
